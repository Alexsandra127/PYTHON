# Solicita os valores iniciais
divida_inicial = float(input("  Digite o valor inicial da divida: "))
taxa_juros = float(input("Digite o valor dos juros mensal: "))
pago_mensal = float(input("Digite o valor que será pago mensalmente: "))

# Inicializa variáveis
meses
